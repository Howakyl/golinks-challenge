import classes from "./CommitLinks.module.css";

const CommitLinks = (props) => {
  return (
    <div>
      <button>Code</button>
      <button>Hash</button>
    </div>
  );
};

export default CommitLinks;
